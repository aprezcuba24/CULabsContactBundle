{% extends 'CULabsAdminBundle:Crud:index.html.twig' %}

{% block title_page %}{{ 'MensajeMasivo list'|trans}}{% endblock %}

{% block list_id %}MensajeMasivo_list{% endblock %}

{% block filter_form %}
<form action="{{ path('admin_mensajemasivo_filter') }}" method="post">
    <div class="form">
        <div>
            {% form_theme filter 'CULabsAdminBundle:Form:fields.html.twig' %}
            {{  form_rest(filter) }}
            <div style="clear: both"></div>
        </div>
        <input class="btn btn-primary" type="submit" name="action_filter" value="{{ 'Filter'|trans }}"/>
        <input class="btn" type="submit" name="action_reset" value="{{ 'Reset'|trans }}" />
    </div>
</form>
{% endblock %}

{% block actions %}
    <ul class="actions">
        {% if is_granted('ROLE_MENSAJEMASIVO_NEW') %}
        <li>
            <a href="{{ path('admin_mensajemasivo_new') }}" class="btn btn-icon btn-primary">     
              <i class="icon-plus-sign icon-white"></i>{{ 'New'|trans }}  
            </a>
        </li>
        {% endif %}
    </ul>
{% endblock %}

{% block list %}
<div id="ajax_MensajeMasivo">
    {% include 'CULabsContactBundle:MensajeMasivoCRUD:list.html.twig' with { 
        'pager': pager,
        'sort' : sort,
    } %}
</div>
{% endblock %}
